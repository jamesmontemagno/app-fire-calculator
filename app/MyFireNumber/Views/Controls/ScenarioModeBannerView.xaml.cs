namespace MyFireNumber.Views.Controls;

/// <summary>
/// Shows that a calculator scenario reads its compatible values from Profile, and offers the
/// unlink action. Defined in XAML so the banner uses the app's light and dark theme resources
/// rather than fixed colors.
/// </summary>
public partial class ScenarioModeBannerView : ContentView
{
    public ScenarioModeBannerView()
    {
        InitializeComponent();
    }
}

<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:MyFireNumber.Views.Controls"
             xmlns:viewModels="clr-namespace:MyFireNumber.ViewModels"
             x:Class="MyFireNumber.Views.OnboardingChoicePage"
             x:DataType="viewModels:OnboardingChoiceViewModel"
             Title="Choose a starting point"
             Shell.TabBarIsVisible="False"
             Background="{AppThemeBinding Light={StaticResource SurfacePageLight}, Dark={StaticResource SurfacePageDark}}"
             SafeAreaEdges="Container">
    <Grid>
        <ScrollView>
            <VerticalStackLayout Padding="{StaticResource PagePadding}" Spacing="{StaticResource SpaceLg}">
                <VerticalStackLayout Spacing="6">
                    <Label Text="Choose your starting point"
                           FontSize="28"
                           FontAttributes="Bold"
                           LineHeight="1.1"
                           TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}"
                           SemanticProperties.HeadingLevel="Level1" />
                    <Label Text="Your defaults are saved. You can refine them any time in Settings."
                           LineHeight="1.3"
                           TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}" />
                </VerticalStackLayout>

                <Border Padding="{StaticResource CardPadding}"
                        Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}"
                        Stroke="{AppThemeBinding Light={StaticResource BorderSubtleLight}, Dark={StaticResource BorderSubtleDark}}"
                        StrokeShape="RoundRectangle 12">
                    <VerticalStackLayout Spacing="{StaticResource SpaceMd}">
                        <Label Text="Take the FIRE Quiz"
                               FontSize="20"
                               FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" />
                        <Label Text="Answer a few questions to find a starting path and compare alternatives."
                               LineHeight="1.3"
                               TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}" />
                        <Button AutomationId="OnboardingTakeQuizButton"
                                Text="Take the FIRE Quiz"
                                Command="{Binding TakeQuizCommand}"
                                Background="{StaticResource FixedAccent}"
                                TextColor="{StaticResource FixedWhite}"
                                SemanticProperties.Description="Take the FIRE Quiz to find a starting path and compare alternatives." />
                    </VerticalStackLayout>
                </Border>

                <Button AutomationId="OnboardingStartStandardFireButton"
                        Text="Start with Standard FIRE"
                        Command="{Binding StartStandardFireCommand}"
                        Background="Transparent"
                        BorderColor="{AppThemeBinding Light={StaticResource TextAccentLight}, Dark={StaticResource TextAccentDark}}"
                        BorderWidth="1"
                        TextColor="{AppThemeBinding Light={StaticResource TextAccentAltLight}, Dark={StaticResource TextAccentAltDark}}"
                        SemanticProperties.Description="Open the Standard FIRE calculator with your saved defaults." />
                <Button AutomationId="OnboardingExploreOnMyOwnButton"
                        Text="Explore on my own"
                        Command="{Binding ExploreOnMyOwnCommand}"
                        Background="Transparent"
                        TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"
                        SemanticProperties.Description="Finish setup and explore all calculators on your own." />

                <controls:EducationalDisclaimerView />
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>

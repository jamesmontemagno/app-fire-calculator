<?xml version="1.0" encoding="utf-8" ?>
<views:CalculatorPageBase xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                          xmlns:controls="clr-namespace:MyFireNumber.Views.Controls"
                          xmlns:views="clr-namespace:MyFireNumber.Views"
                          xmlns:viewModels="clr-namespace:MyFireNumber.ViewModels"
                          xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
                          x:Class="MyFireNumber.Views.DebtPayoffPage"
                          x:DataType="viewModels:DebtPayoffViewModel"
                          Title="{Binding Title}"
                          Shell.NavBarIsVisible="True"
                          Background="{AppThemeBinding Light={StaticResource SurfacePageLight}, Dark={StaticResource SurfacePageDark}}"
                          SafeAreaEdges="All">
    <ContentPage.ToolbarItems>
        <ToolbarItem Command="{Binding SavePlanAndCloseCommand}"
                     SemanticProperties.Description="Save the current calculator values as a plan.">
            <ToolbarItem.IconImageSource>
                <FontImageSource FontFamily="FontAwesomeSolid" Glyph="&#xf0c7;" Color="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Size="18" />
            </ToolbarItem.IconImageSource>
        </ToolbarItem>
    </ContentPage.ToolbarItems>
    <ScrollView>
        <VerticalStackLayout Padding="{StaticResource PagePadding}" Spacing="{StaticResource SpaceLg}">
            <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" HorizontalOptions="Center" SemanticProperties.Description="Restoring your local calculator draft." />
            <Border IsVisible="{Binding HasValidationMessage}" Padding="14" Background="{AppThemeBinding Light={StaticResource SurfaceWarningLight}, Dark={StaticResource SurfaceWarningDark}}" Stroke="{AppThemeBinding Light={StaticResource BorderWarningLight}, Dark={StaticResource BorderWarningDark}}" StrokeShape="RoundRectangle 8"><Label Text="{Binding ValidationMessage}" TextColor="{AppThemeBinding Light={StaticResource TextWarningLight}, Dark={StaticResource TextWarningDark}}" LineHeight="1.3" /></Border>

            <controls:CalculatorInfoView Title="What is a debt payoff strategy?"
                                         Summary="Compare Snowball and Avalanche plans to eliminate multiple debts more intentionally."
                                         Details="Snowball prioritizes the smallest balance for early milestones. Avalanche prioritizes the highest rate and can reduce total interest."
                                         IconGlyph="&#xf09d;" />

            <Border Padding="{StaticResource CardPadding}" Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}" Stroke="{AppThemeBinding Light={StaticResource BorderSubtleLight}, Dark={StaticResource BorderSubtleDark}}" StrokeShape="RoundRectangle 8">
                <VerticalStackLayout Spacing="{StaticResource SpaceMd}">
                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="{StaticResource SpaceSm}">
                        <Label Text="Your debts" Style="{StaticResource CalculatorSectionHeading}" />
                        <Button Grid.Column="1" AutomationId="AddDebtButton" Text="Add" Command="{Binding AddDebtCommand}" IsVisible="{Binding CanEditProfileOwnedFields}" Background="{StaticResource Secondary}" TextColor="{StaticResource PrimaryDarkText}" FontSize="12" SemanticProperties.Description="Add another debt to this payoff plan."><Button.ImageSource><FontImageSource FontFamily="FontAwesomeSolid" Glyph="&#xf067;" Color="{StaticResource PrimaryDarkText}" Size="12" /></Button.ImageSource></Button>
                    </Grid>
                    <controls:FieldHelpView Header="Debt details" HelpText="For each debt, enter its balance, annual interest rate, and required minimum payment." />
                    <VerticalStackLayout BindableLayout.ItemsSource="{Binding DebtItems}" Spacing="{StaticResource SpaceSm}">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate x:DataType="viewModels:DebtEditorItem">
                                <Border Padding="{StaticResource CardPaddingCompact}" Background="{AppThemeBinding Light={StaticResource SurfaceWarningSoftLight}, Dark={StaticResource SurfaceWarningSoftDark}}" Stroke="{AppThemeBinding Light={StaticResource BorderWarningSoftLight}, Dark={StaticResource BorderWarningSoftDark}}" StrokeShape="RoundRectangle 8">
                                    <VerticalStackLayout Spacing="{StaticResource SpaceSm}">
                                        <Grid ColumnDefinitions="*,44" ColumnSpacing="{StaticResource SpaceSm}">
                                            <Entry Text="{Binding Name}" IsEnabled="{Binding IsEditable}" Placeholder="Debt name" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" SemanticProperties.Description="Debt name" />
                                            <Button Grid.Column="1" AutomationId="RemoveDebtButton" Text="&#xf2ed;" FontFamily="FontAwesomeSolid" IsVisible="{Binding IsEditable}" Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.RemoveDebtCommand}" CommandParameter="{Binding .}" Background="{StaticResource FixedDangerSurface}" TextColor="{StaticResource FixedDanger}" Padding="0" SemanticProperties.Description="Remove this debt from the payoff plan." />
                                        </Grid>
                                        <controls:NumericInputView Header="Balance"
                                                                   IsEnabled="{Binding IsEditable}"
                                                                   HelpText="Current amount owed. Enter the exact balance because useful debt ranges vary too widely for a slider."
                                                                   Placeholder="5000"
                                                                   Text="{Binding BalanceText, Mode=TwoWay}" />
                                        <controls:PercentageInputView AutomationId="DebtRateInput"
                                                                      Header="Interest rate"
                                                                      IsEnabled="{Binding IsEditable}"
                                                                      HelpText="Annual percentage rate, adjustable in quarter-point increments."
                                                                      Placeholder="19.99"
                                                                      Minimum="0"
                                                                      Maximum="100"
                                                                      Step="0.25"
                                                                      Text="{Binding RateText, Mode=TwoWay}" />
                                        <controls:NumericInputView Header="Minimum monthly payment"
                                                                   IsEnabled="{Binding IsEditable}"
                                                                   HelpText="Required minimum payment. Enter the exact amount because it depends on the lender and balance."
                                                                   Placeholder="150"
                                                                   PeriodSuffix="/mo"
                                                                   Text="{Binding MinimumPaymentText, Mode=TwoWay}" />
                                    </VerticalStackLayout>
                                </Border>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>

            <Border Padding="{StaticResource CardPadding}" Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}" Stroke="{AppThemeBinding Light={StaticResource BorderSubtleLight}, Dark={StaticResource BorderSubtleDark}}" StrokeShape="RoundRectangle 8">
                <VerticalStackLayout Spacing="{StaticResource SpaceMd}">
                    <Label Text="Payoff strategy" Style="{StaticResource CalculatorSectionHeading}" />
                    <Label Text="Choose how much to pay and which debt to prioritize first." Style="{StaticResource CalculatorSupportingText}" />
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="{StaticResource SpaceSm}">
                        <Button Text="Fixed budget" Command="{Binding SetDebtPayoffModeCommand}" CommandParameter="fixed" Background="{StaticResource Secondary}" TextColor="{StaticResource PrimaryDarkText}" SemanticProperties.Description="Use a fixed total monthly debt budget."><Button.Triggers><DataTrigger TargetType="Button" Binding="{Binding IsFixedDebtPayoff}" Value="True"><Setter Property="Background" Value="{StaticResource Primary}" /><Setter Property="TextColor" Value="{StaticResource White}" /></DataTrigger></Button.Triggers></Button>
                        <Button Grid.Column="1" Text="Target timeline" Command="{Binding SetDebtPayoffModeCommand}" CommandParameter="target" Background="{StaticResource Secondary}" TextColor="{StaticResource PrimaryDarkText}" SemanticProperties.Description="Calculate the payment required for a target payoff timeline."><Button.Triggers><DataTrigger TargetType="Button" Binding="{Binding IsTargetDebtPayoff}" Value="True"><Setter Property="Background" Value="{StaticResource Primary}" /><Setter Property="TextColor" Value="{StaticResource White}" /></DataTrigger></Button.Triggers></Button>
                    </Grid>
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="{StaticResource SpaceSm}">
                        <Button Text="Snowball" Command="{Binding SetDebtStrategyCommand}" CommandParameter="snowball" Background="{StaticResource Secondary}" TextColor="{StaticResource PrimaryDarkText}" SemanticProperties.Description="Pay the smallest debt first."><Button.Triggers><DataTrigger TargetType="Button" Binding="{Binding IsSnowballStrategy}" Value="True"><Setter Property="Background" Value="{StaticResource Primary}" /><Setter Property="TextColor" Value="{StaticResource White}" /></DataTrigger></Button.Triggers></Button>
                        <Button Grid.Column="1" Text="Avalanche" Command="{Binding SetDebtStrategyCommand}" CommandParameter="avalanche" Background="{StaticResource Secondary}" TextColor="{StaticResource PrimaryDarkText}" SemanticProperties.Description="Pay the highest interest rate first."><Button.Triggers><DataTrigger TargetType="Button" Binding="{Binding IsAvalancheStrategy}" Value="True"><Setter Property="Background" Value="{StaticResource Primary}" /><Setter Property="TextColor" Value="{StaticResource White}" /></DataTrigger></Button.Triggers></Button>
                    </Grid>
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="{StaticResource SpaceMd}">
                        <controls:NumericInputView IsVisible="{Binding IsFixedDebtPayoff}" Header="Monthly debt budget" HelpText="Total amount available for debt payments each month, in dollars." Placeholder="1000" Text="{Binding DebtMonthlyBudgetText}" />
                        <controls:NumericInputView IsVisible="{Binding IsTargetDebtPayoff}" Header="Target months" HelpText="Months in which you aim to eliminate all debt." Placeholder="36" Text="{Binding DebtTargetMonthsText}" />
                        <controls:NumericInputView Grid.Column="1" Header="Additional what-if payment" HelpText="Optional amount to add on top of your current monthly debt budget." Placeholder="0" Text="{Binding DebtExtraPaymentText}" />
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <VerticalStackLayout Spacing="{StaticResource SpaceSm}">
                <Label Text="Your payoff outlook" Style="{StaticResource CalculatorSectionHeading}" />
                <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto,Auto" ColumnSpacing="{StaticResource SpaceSm}" RowSpacing="{StaticResource SpaceSm}">
                    <Border Padding="{StaticResource CardPaddingCompact}" Background="{AppThemeBinding Light={StaticResource SurfaceWarningAltLight}, Dark={StaticResource SurfaceWarningAltDark}}" StrokeThickness="0" StrokeShape="RoundRectangle 8"><VerticalStackLayout Spacing="2"><Label Text="Total debt" FontSize="12" /><Label Text="{Binding DebtTotalText}" FontAttributes="Bold" FontSize="22" /></VerticalStackLayout></Border>
                    <Border Grid.Column="1" Padding="{StaticResource CardPaddingCompact}" Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}" StrokeShape="RoundRectangle 8"><VerticalStackLayout Spacing="2"><Label Text="Minimum payments" FontSize="12" /><Label Text="{Binding DebtMinimumPaymentsText}" FontAttributes="Bold" FontSize="18" /></VerticalStackLayout></Border>
                    <Border Grid.Row="1" Padding="{StaticResource CardPaddingCompact}" Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}" StrokeShape="RoundRectangle 8"><VerticalStackLayout Spacing="2"><Label Text="Payoff time" FontSize="12" /><Label Text="{Binding DebtPayoffTimeText}" FontAttributes="Bold" FontSize="18" /></VerticalStackLayout></Border>
                    <Border Grid.Row="1" Grid.Column="1" Padding="{StaticResource CardPaddingCompact}" Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}" StrokeShape="RoundRectangle 8"><VerticalStackLayout Spacing="2"><Label Text="Total interest" FontSize="12" /><Label Text="{Binding DebtInterestText}" FontAttributes="Bold" FontSize="18" /></VerticalStackLayout></Border>
                    <Border Grid.Row="2" Grid.ColumnSpan="2" Padding="{StaticResource CardPaddingCompact}" Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}" StrokeShape="RoundRectangle 8"><VerticalStackLayout Spacing="2"><Label Text="Monthly payment" FontSize="12" /><Label Text="{Binding DebtPaymentText}" FontAttributes="Bold" FontSize="18" /></VerticalStackLayout></Border>
                </Grid>
                <Label Text="{Binding DebtStrategySummary}" Style="{StaticResource CalculatorSupportingText}" />
            </VerticalStackLayout>

            <VerticalStackLayout Spacing="{StaticResource SpaceSm}">
                <Label Text="Debt balance projection" Style="{StaticResource CalculatorSectionHeading}" />
                <lvc:CartesianChart Series="{Binding ProjectionSeries}" XAxes="{Binding ProjectionXAxes}" AnimationsSpeed="{Binding ChartAnimationsSpeed}" HeightRequest="300" SemanticProperties.Description="{Binding ProjectionChartDescription}" />
                <Label Text="Principal and interest paid" Style="{StaticResource CalculatorSectionHeading}" />
                <lvc:CartesianChart Series="{Binding DebtBreakdownSeries}" XAxes="{Binding ProjectionXAxes}" AnimationsSpeed="{Binding ChartAnimationsSpeed}" HeightRequest="300" SemanticProperties.Description="{Binding DebtBreakdownDescription}" />
                <Label Text="{Binding DebtBreakdownSummary}" Style="{StaticResource CalculatorSupportingText}" />
                <Grid ColumnDefinitions="*,*" ColumnSpacing="{StaticResource SpaceSm}"><VerticalStackLayout Spacing="2"><Label Text="Snowball" FontAttributes="Bold" /><Label Text="{Binding DebtSnowballComparisonText}" Style="{StaticResource CalculatorSupportingText}" /></VerticalStackLayout><VerticalStackLayout Grid.Column="1" Spacing="2"><Label Text="Avalanche" FontAttributes="Bold" /><Label Text="{Binding DebtAvalancheComparisonText}" Style="{StaticResource CalculatorSupportingText}" /></VerticalStackLayout></Grid>
            </VerticalStackLayout>

            <Border Padding="{StaticResource CardPadding}" Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}" Stroke="{AppThemeBinding Light={StaticResource BorderSubtleLight}, Dark={StaticResource BorderSubtleDark}}" StrokeShape="RoundRectangle 8">
                <VerticalStackLayout Spacing="{StaticResource SpaceSm}">
                    <Label Text="Save or export this plan" Style="{StaticResource CalculatorSectionHeading}" />
                    <Entry Text="{Binding PlanNameText}" Placeholder="My Debt Payoff Plan" Background="{AppThemeBinding Light={StaticResource SurfacePageLight}, Dark={StaticResource SurfacePageDark}}" SemanticProperties.Description="Name for this saved Debt Payoff plan" />
                    <Button Text="{Binding SavePlanActionText}" Command="{Binding SavePlanAndCloseCommand}" SemanticProperties.Description="{Binding SavePlanActionDescription}" />
                    <Button Text="Export to Excel" Command="{Binding ExportCommand}" Background="{StaticResource Secondary}" TextColor="{StaticResource PrimaryDarkText}" SemanticProperties.Description="Create a Debt Payoff workbook on this device and open the native share sheet." />
                    <Label IsVisible="{Binding HasPlanStatusMessage}" Text="{Binding PlanStatusMessage}" Style="{StaticResource CalculatorSupportingText}" />
                    <Label IsVisible="{Binding HasExportStatusMessage}" Text="{Binding ExportStatusMessage}" Style="{StaticResource CalculatorSupportingText}" />
                    <controls:EducationalDisclaimerView />
                </VerticalStackLayout>
            </Border>
            <controls:CalculationExplanationView Explanation="Each month, interest charge = current balance x (annual interest rate / 12), then the charge is added to the balance.&#10;&#10;Available monthly budget = total monthly payment + extra payment.&#10;&#10;Minimum payments are applied in priority order, and remaining budget is applied as principal to the first unpaid debt. Snowball priority sorts by smallest balance; Avalanche priority sorts by highest interest rate.&#10;&#10;Monthly principal = total payments - monthly interest. The schedule repeats until all balances are paid or 600 months have elapsed." />
        </VerticalStackLayout>
    </ScrollView>
</views:CalculatorPageBase>

<?xml version="1.0" encoding="utf-8" ?>
<views:CalculatorPageBase xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:MyFireNumber.Views.Controls"
             xmlns:views="clr-namespace:MyFireNumber.Views"
             xmlns:viewModels="clr-namespace:MyFireNumber.ViewModels"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
             x:Class="MyFireNumber.Views.RetirementCashFlowPage"
             x:DataType="viewModels:RetirementCashFlowViewModel"
             Title="{Binding Title}"
             Shell.NavBarIsVisible="True"
             Background="{AppThemeBinding Light={StaticResource SurfacePageLight}, Dark={StaticResource SurfacePageDark}}"
             SafeAreaEdges="All">
    <views:CalculatorPageBase.Resources>
        <controls:NumericTextConverter x:Key="NumericTextConverter" />
    </views:CalculatorPageBase.Resources>

    <ContentPage.ToolbarItems>
        <ToolbarItem Command="{Binding SavePlanAndCloseCommand}"
                     SemanticProperties.Description="Save the current calculator values as a plan.">
            <ToolbarItem.IconImageSource>
                <FontImageSource FontFamily="FontAwesomeSolid" Glyph="&#xf0c7;" Color="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Size="18" />
            </ToolbarItem.IconImageSource>
        </ToolbarItem>
    </ContentPage.ToolbarItems>
    <ScrollView>
        <VerticalStackLayout Padding="{StaticResource PagePadding}" Spacing="{StaticResource SpaceLg}">
            <ActivityIndicator IsRunning="{Binding IsLoading}"
                               IsVisible="{Binding IsLoading}"
                               HorizontalOptions="Center"
                               SemanticProperties.Description="Restoring your local calculator draft." />

            <Border IsVisible="{Binding HasValidationMessage}"
                    Padding="14"
                    Background="{AppThemeBinding Light={StaticResource SurfaceWarningLight}, Dark={StaticResource SurfaceWarningDark}}"
                    Stroke="{AppThemeBinding Light={StaticResource BorderWarningLight}, Dark={StaticResource BorderWarningDark}}"
                    StrokeShape="RoundRectangle 8">
                <Label Text="{Binding ValidationMessage}"
                       TextColor="{AppThemeBinding Light={StaticResource TextWarningLight}, Dark={StaticResource TextWarningDark}}"
                       LineHeight="1.3" />
            </Border>

                <controls:CalculatorInfoView Title="What is retirement cash flow planning?"
                                             Summary="Coordinate accounts, deferred payouts, earned income, and expenses in one retirement timeline."
                                             Details="Retirement cash flow planning shows when money enters and leaves your plan year by year, including deferred compensation, retirement accounts, savings, part-time income, and expenses. It can reveal funding gaps that a single portfolio target may miss. It may fit people with complex, multi-account or phased-retirement plans."
                                             IconGlyph="&#xf1da;" />
                <Border Padding="{StaticResource CardPadding}" Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}" Stroke="{AppThemeBinding Light={StaticResource BorderSubtleLight}, Dark={StaticResource BorderSubtleDark}}" StrokeShape="RoundRectangle 8">
                    <VerticalStackLayout Spacing="{StaticResource SpaceMd}">
                        <Label Text="Scenario" Style="{StaticResource CalculatorSectionHeading}" />
                        <controls:PeriodToggleView />
                        <Grid ColumnDefinitions="*,*" ColumnSpacing="{StaticResource SpaceMd}">
                            <controls:NumericInputView Header="Current age" IsEnabled="{Binding CanEditProfileOwnedFields}" HelpText="Your age today, in years." Placeholder="45" Text="{Binding RetirementCurrentAgeText}" />
                            <controls:NumericInputView Grid.Column="1" Header="Semi-retirement age" IsEnabled="{Binding CanEditProfileOwnedFields}" HelpText="The age when your plan changes from full-time work." Placeholder="55" Text="{Binding RetirementSemiAgeText}" />
                        </Grid>
                        <controls:NumericInputView Header="Expenses" IsEnabled="{Binding CanEditProfileOwnedFields}" HelpText="Expected spending in retirement. Itemized Profile expenses replace this aggregate amount in linked mode." Placeholder="80000" Text="{Binding RetirementExpensesText}" PeriodSuffix="{Binding DisplayPeriodSuffix}" />
                        <controls:AdvancedAssumptionsExpander>
                            <VerticalStackLayout Margin="0,12,0,0" Spacing="{StaticResource SpaceMd}">
                                <Grid ColumnDefinitions="*,*" ColumnSpacing="{StaticResource SpaceMd}">
                                    <VerticalStackLayout Spacing="0">
                                        <controls:NumericInputView Header="Plan through age"
                                                                   HelpText="The last age included in the cash-flow plan."
                                                                   Placeholder="90"
                                                                   Text="{Binding RetirementPlanThroughAgeText}" />
                                        <controls:SteppedSlider AutomationId="RetirementPlanThroughAgeSlider"
                                                                Minimum="{Binding RetirementPlanThroughMinimum}"
                                                                Maximum="100"
                                                                StepSize="1"
                                                                Value="{Binding RetirementPlanThroughAgeText, Mode=TwoWay, Converter={StaticResource NumericTextConverter}}"
                                                                SemanticProperties.Description="Plan-through age, adjustable in one-year increments." />
                                    </VerticalStackLayout>
                                    <controls:PercentageInputView Grid.Column="1" Header="Inflation rate" HelpText="Expected annual increase in expenses." Placeholder="3" Minimum="0" Maximum="10" Step="0.1" Text="{Binding RetirementInflationText}" />
                                </Grid>
                                <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*,Auto" RowSpacing="{StaticResource SpaceSm}">
                                    <Label Text="Withdraw only after semi-retirement" VerticalTextAlignment="Center" />
                                    <Switch Grid.Column="1" IsToggled="{Binding WithdrawOnlyAfterRetirement}" SemanticProperties.Description="Withdraw from accounts only after semi-retirement" />
                                    <Label Grid.Row="1" Text="Reinvest annual surplus" VerticalTextAlignment="Center" />
                                    <Switch Grid.Row="1" Grid.Column="1" IsToggled="{Binding ReinvestRetirementSurplus}" SemanticProperties.Description="Reinvest annual retirement cash-flow surplus" />
                                </Grid>
                                <Label Text="Add only the accounts, income, and expenses that apply to your plan." Style="{StaticResource CalculatorSupportingText}" />
                            </VerticalStackLayout>
                        </controls:AdvancedAssumptionsExpander>
                    </VerticalStackLayout>
                </Border>

                <Border Padding="{StaticResource CardPadding}"
                        Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}"
                        Stroke="{AppThemeBinding Light={StaticResource BorderSubtleLight}, Dark={StaticResource BorderSubtleDark}}"
                        StrokeShape="RoundRectangle 8">
                    <VerticalStackLayout Spacing="{StaticResource SpaceMd}">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="{StaticResource SpaceSm}">
                            <Label Text="&#xf1c0;" FontFamily="FontAwesomeSolid" VerticalTextAlignment="Center" TextColor="{AppThemeBinding Light={StaticResource TextWarningAltLight}, Dark={StaticResource TextWarningAltDark}}" SemanticProperties.Description="Retirement accounts" />
                            <Label Grid.Column="1" Text="Accounts" Style="{StaticResource CalculatorSectionHeading}" />
                            <Button Grid.Column="2" AutomationId="AddRetirementAccountButton" Text="Add" IsVisible="{Binding CanEditProfileOwnedFields}" Command="{Binding AddRetirementAccountCommand}" Background="{StaticResource FixedWarningSurface}" TextColor="{StaticResource FixedWarning}" FontSize="12" SemanticProperties.Description="Add a retirement account.">
                                <Button.ImageSource><FontImageSource FontFamily="FontAwesomeSolid" Glyph="&#xf067;" Color="{StaticResource FixedWarning}" Size="12" /></Button.ImageSource>
                            </Button>
                        </Grid>
                        <VerticalStackLayout BindableLayout.ItemsSource="{Binding RetirementAccounts}" Spacing="{StaticResource SpaceSm}">
                            <BindableLayout.EmptyView><Label Text="No accounts yet. Add one to model its balance, return, availability, and taxes." Style="{StaticResource CalculatorSupportingText}" /></BindableLayout.EmptyView>
                            <BindableLayout.ItemTemplate>
                                <DataTemplate x:DataType="viewModels:RetirementAccountEditorItem">
                                    <Border Padding="{StaticResource CardPaddingCompact}" Background="{AppThemeBinding Light={StaticResource SurfaceWarningSoftLight}, Dark={StaticResource SurfaceWarningSoftDark}}" Stroke="{AppThemeBinding Light={StaticResource BorderWarningSoftLight}, Dark={StaticResource BorderWarningSoftDark}}" StrokeShape="RoundRectangle 8">
                                        <VerticalStackLayout Spacing="{StaticResource SpaceSm}">
                                            <Grid ColumnDefinitions="*,44,44" ColumnSpacing="{StaticResource SpaceSm}">
                                                <Label Text="{Binding Name}" FontAttributes="Bold" VerticalTextAlignment="Center" TextColor="{AppThemeBinding Light={StaticResource TextWarningLight}, Dark={StaticResource TextWarningDark}}" SemanticProperties.Description="{Binding Name, StringFormat='Expand or collapse {0} account.'}" />
                                                <Button Grid.Column="1" Text="{Binding ExpansionGlyph}" FontFamily="FontAwesomeSolid" Command="{Binding ToggleExpandedCommand}" Background="Transparent" TextColor="{AppThemeBinding Light={StaticResource TextWarningAltLight}, Dark={StaticResource TextWarningAltDark}}" Padding="0" SemanticProperties.Description="{Binding Name, StringFormat='Expand or collapse {0} account.'}" />
                                                <Button Grid.Column="2" AutomationId="RemoveRetirementAccountButton" Text="&#xf2ed;" FontFamily="FontAwesomeSolid" IsVisible="{Binding IsEditable}" Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.RemoveRetirementAccountCommand}" CommandParameter="{Binding .}" Background="{StaticResource FixedDangerSurface}" TextColor="{StaticResource FixedDanger}" Padding="0" SemanticProperties.Description="Remove this retirement account." />
                                            </Grid>
                                            <VerticalStackLayout IsVisible="{Binding IsExpanded}" IsEnabled="{Binding IsEditable}" Spacing="{StaticResource SpaceSm}">
                                            <Entry Text="{Binding Name, Mode=TwoWay}" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="Account name" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Retirement account name" />
                                            <Picker Title="Account type" ItemsSource="{Binding AccountTypes}" SelectedItem="{Binding Type, Mode=TwoWay}" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" TitleColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Retirement account type" />
                                            <Grid ColumnDefinitions="*,*" ColumnSpacing="{StaticResource SpaceSm}">
                                                <VerticalStackLayout Spacing="3"><Label Text="Balance" FontAttributes="Bold" FontSize="12" /><Entry Text="{Binding BalanceText, Mode=TwoWay}" Keyboard="Numeric" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="0" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Current account balance in dollars" /></VerticalStackLayout>
                                                <VerticalStackLayout Grid.Column="1" Spacing="3"><Label Text="Annual contribution" FontAttributes="Bold" FontSize="12" /><Entry Text="{Binding AnnualContributionText, Mode=TwoWay}" Keyboard="Numeric" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="0" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Annual account contribution in dollars" /></VerticalStackLayout>
                                            </Grid>
                                            <Grid ColumnDefinitions="*,*" ColumnSpacing="{StaticResource SpaceSm}">
                                                <VerticalStackLayout Spacing="3"><Label Text="Return (%)" FontAttributes="Bold" FontSize="12" /><Entry Text="{Binding AnnualReturnText, Mode=TwoWay}" Keyboard="Numeric" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="5" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Expected annual account return percentage" /></VerticalStackLayout>
                                                <VerticalStackLayout Grid.Column="1" Spacing="3"><Label Text="Available age" FontAttributes="Bold" FontSize="12" /><Entry Text="{Binding AvailableAgeText, Mode=TwoWay}" Keyboard="Numeric" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="59" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Age when this account becomes available" /></VerticalStackLayout>
                                            </Grid>
                                            <VerticalStackLayout IsVisible="{Binding UsesWithdrawalRate}" Spacing="3"><Label Text="Withdrawal rate (%)" FontAttributes="Bold" FontSize="12" /><Label Text="A spending policy, not a hard limit: this share of the remaining balance is taken each year, and the plan only goes above it in a year that would otherwise fall short." FontSize="11" /><Entry Text="{Binding WithdrawalRateText, Mode=TwoWay}" Keyboard="Numeric" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="4" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Annual account withdrawal rate percentage" /></VerticalStackLayout>
                                            <VerticalStackLayout IsVisible="{Binding UsesPayoutSchedule}" Spacing="3"><Label Text="Payout years" FontAttributes="Bold" FontSize="12" /><Label Text="The undistributed balance keeps earning its return during payout, so each year distributes the remaining balance over the remaining payout years." FontSize="11" /><Entry AutomationId="DeferredPayoutYearsEntry" Text="{Binding PayoutYearsText, Mode=TwoWay}" Keyboard="Numeric" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="5" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Number of years receiving equal deferred compensation payouts" /></VerticalStackLayout>
                                            <VerticalStackLayout Spacing="3"><Label Text="Withdrawal tax rate (%)" FontAttributes="Bold" FontSize="12" /><Entry AutomationId="WithdrawalTaxRateEntry" Text="{Binding WithdrawalTaxRateText, Mode=TwoWay}" Keyboard="Numeric" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="0" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Flat estimated tax rate applied to withdrawals from this account" /><Label Text="{Binding WithdrawalTaxHelpText}" FontSize="11" /></VerticalStackLayout>
                                            </VerticalStackLayout>
                                        </VerticalStackLayout>
                                    </Border>
                                </DataTemplate>
                            </BindableLayout.ItemTemplate>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Border>

                <Border Padding="{StaticResource CardPadding}"
                        Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}"
                        Stroke="{AppThemeBinding Light={StaticResource BorderSubtleLight}, Dark={StaticResource BorderSubtleDark}}"
                        StrokeShape="RoundRectangle 8">
                    <VerticalStackLayout Spacing="{StaticResource SpaceMd}">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="{StaticResource SpaceSm}">
                            <Label Text="&#xf0d6;" FontFamily="FontAwesomeSolid" VerticalTextAlignment="Center" TextColor="{AppThemeBinding Light={StaticResource TextAccentLight}, Dark={StaticResource TextAccentDark}}" SemanticProperties.Description="Retirement income" />
                            <Label Grid.Column="1" Text="Income sources" Style="{StaticResource CalculatorSectionHeading}" />
                            <Button Grid.Column="2" AutomationId="AddRetirementIncomeButton" Text="Add" IsVisible="{Binding CanEditProfileOwnedFields}" Command="{Binding AddRetirementIncomeCommand}" Background="{StaticResource FixedAccentSurface}" TextColor="{StaticResource FixedAccentStrong}" FontSize="12" SemanticProperties.Description="Add a retirement income source.">
                                <Button.ImageSource><FontImageSource FontFamily="FontAwesomeSolid" Glyph="&#xf067;" Color="{StaticResource FixedAccentStrong}" Size="12" /></Button.ImageSource>
                            </Button>
                        </Grid>
                        <VerticalStackLayout BindableLayout.ItemsSource="{Binding RetirementIncomeSources}" Spacing="{StaticResource SpaceSm}">
                            <BindableLayout.EmptyView><Label Text="No income sources yet. Add one and choose whether its amount is before or after tax." Style="{StaticResource CalculatorSupportingText}" /></BindableLayout.EmptyView>
                            <BindableLayout.ItemTemplate>
                                <DataTemplate x:DataType="viewModels:RetirementIncomeEditorItem">
                                    <Border Padding="{StaticResource CardPaddingCompact}" Background="{AppThemeBinding Light={StaticResource SurfaceAccentTintPaleLight}, Dark={StaticResource SurfaceAccentTintPaleDark}}" Stroke="{AppThemeBinding Light={StaticResource BorderAccentAltLight}, Dark={StaticResource BorderAccentAltDark}}" StrokeShape="RoundRectangle 8">
                                        <VerticalStackLayout Spacing="{StaticResource SpaceSm}">
                                            <Grid ColumnDefinitions="*,44,44" ColumnSpacing="{StaticResource SpaceSm}">
                                                <Label Text="{Binding Name}" FontAttributes="Bold" VerticalTextAlignment="Center" TextColor="{AppThemeBinding Light={StaticResource TextAccentStrongLight}, Dark={StaticResource TextAccentStrongDark}}" SemanticProperties.Description="{Binding Name, StringFormat='Expand or collapse {0} income source.'}" />
                                                <Button Grid.Column="1" Text="{Binding ExpansionGlyph}" FontFamily="FontAwesomeSolid" Command="{Binding ToggleExpandedCommand}" Background="Transparent" TextColor="{AppThemeBinding Light={StaticResource TextAccentLight}, Dark={StaticResource TextAccentDark}}" Padding="0" SemanticProperties.Description="{Binding Name, StringFormat='Expand or collapse {0} income source.'}" />
                                                <Button Grid.Column="2" AutomationId="RemoveRetirementIncomeButton" Text="&#xf2ed;" FontFamily="FontAwesomeSolid" IsVisible="{Binding IsEditable}" Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.RemoveRetirementIncomeCommand}" CommandParameter="{Binding .}" Background="{StaticResource FixedDangerSurface}" TextColor="{StaticResource FixedDanger}" Padding="0" SemanticProperties.Description="Remove this retirement income source." />
                                            </Grid>
                                            <VerticalStackLayout IsVisible="{Binding IsExpanded}" IsEnabled="{Binding IsEditable}" Spacing="{StaticResource SpaceSm}">
                                            <Entry Text="{Binding Name, Mode=TwoWay}" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="Income name" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Retirement income source name" />
                                            <Grid ColumnDefinitions="*,*" ColumnSpacing="{StaticResource SpaceSm}">
                                                <VerticalStackLayout Spacing="3"><Label Text="Annual amount" FontAttributes="Bold" FontSize="12" /><Entry Text="{Binding AnnualAmountText, Mode=TwoWay}" Keyboard="Numeric" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="0" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Annual retirement income amount in dollars, before or after tax as selected" /></VerticalStackLayout>
                                                <VerticalStackLayout Grid.Column="1" Spacing="3"><Label Text="Growth (%)" FontAttributes="Bold" FontSize="12" /><Entry Text="{Binding AnnualGrowthText, Mode=TwoWay}" Keyboard="Numeric" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="0" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Annual retirement income growth percentage" /></VerticalStackLayout>
                                            </Grid>
                                            <Grid ColumnDefinitions="*,*" ColumnSpacing="{StaticResource SpaceSm}">
                                                <VerticalStackLayout Spacing="3"><Label Text="Start age" FontAttributes="Bold" FontSize="12" /><Entry Text="{Binding StartAgeText, Mode=TwoWay}" Keyboard="Numeric" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="55" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Income start age" /></VerticalStackLayout>
                                                <VerticalStackLayout Grid.Column="1" Spacing="3"><Label Text="End age" FontAttributes="Bold" FontSize="12" /><Entry Text="{Binding EndAgeText, Mode=TwoWay}" Keyboard="Numeric" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="65" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Income end age" /></VerticalStackLayout>
                                            </Grid>
                                            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="{StaticResource SpaceSm}">
                                               <VerticalStackLayout IsVisible="{Binding RequiresTaxRate}" Spacing="3"><Label Text="Tax rate (%)" FontAttributes="Bold" FontSize="12" /><Entry Text="{Binding TaxRateText, Mode=TwoWay}" Keyboard="Numeric" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="0" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Income tax rate percentage" /></VerticalStackLayout>
                                                <HorizontalStackLayout Grid.Column="1" Spacing="{StaticResource SpaceSm}" VerticalOptions="Center"><Label Text="Amount is after tax" VerticalTextAlignment="Center" FontSize="12" /><Switch IsToggled="{Binding IsAfterTax, Mode=TwoWay}" SemanticProperties.Description="Income amount is after tax" /></HorizontalStackLayout>
                                            </Grid>
                                            </VerticalStackLayout>
                                        </VerticalStackLayout>
                                    </Border>
                                </DataTemplate>
                            </BindableLayout.ItemTemplate>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Border>

                <Border Padding="{StaticResource CardPadding}"
                        Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}"
                        Stroke="{AppThemeBinding Light={StaticResource BorderSubtleLight}, Dark={StaticResource BorderSubtleDark}}"
                        StrokeShape="RoundRectangle 8">
                    <VerticalStackLayout Spacing="{StaticResource SpaceMd}">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="{StaticResource SpaceSm}">
                            <Label Text="&#xf53d;" FontFamily="FontAwesomeSolid" VerticalTextAlignment="Center" TextColor="{AppThemeBinding Light={StaticResource TextDangerAltLight}, Dark={StaticResource TextDangerAltDark}}" SemanticProperties.Description="Additional retirement expenses" />
                            <Label Grid.Column="1" Text="Additional expenses" Style="{StaticResource CalculatorSectionHeading}" />
                            <Button Grid.Column="2" AutomationId="AddRetirementExpenseButton" Text="Add" IsVisible="{Binding CanEditProfileOwnedFields}" Command="{Binding AddRetirementExpenseCommand}" Background="{StaticResource FixedDangerSurface}" TextColor="{StaticResource FixedDanger}" FontSize="12" SemanticProperties.Description="Add an additional retirement expense.">
                                <Button.ImageSource><FontImageSource FontFamily="FontAwesomeSolid" Glyph="&#xf067;" Color="{StaticResource FixedDanger}" Size="12" /></Button.ImageSource>
                            </Button>
                        </Grid>
                        <VerticalStackLayout Spacing="{StaticResource SpaceSm}">
                        <VerticalStackLayout BindableLayout.ItemsSource="{Binding RetirementAdditionalExpenses}" Spacing="{StaticResource SpaceSm}">
                            <BindableLayout.EmptyView><Label Text="No additional expenses yet." Style="{StaticResource CalculatorSupportingText}" /></BindableLayout.EmptyView>
                            <BindableLayout.ItemTemplate>
                                <DataTemplate x:DataType="viewModels:RetirementExpenseEditorItem">
                                    <Border Padding="{StaticResource CardPaddingCompact}" Background="{AppThemeBinding Light={StaticResource SurfaceDangerSoftLight}, Dark={StaticResource SurfaceDangerSoftDark}}" Stroke="{AppThemeBinding Light={StaticResource BorderDangerLight}, Dark={StaticResource BorderDangerDark}}" StrokeShape="RoundRectangle 8">
                                        <VerticalStackLayout Spacing="{StaticResource SpaceSm}">
                                            <Grid ColumnDefinitions="*,44,44" ColumnSpacing="{StaticResource SpaceSm}">
                                                <Label Text="{Binding Name}" FontAttributes="Bold" VerticalTextAlignment="Center" TextColor="{AppThemeBinding Light={StaticResource TextDangerLight}, Dark={StaticResource TextDangerDark}}" SemanticProperties.Description="{Binding Name, StringFormat='Expand or collapse {0} additional expense.'}" />
                                                <Button Grid.Column="1" Text="{Binding ExpansionGlyph}" FontFamily="FontAwesomeSolid" Command="{Binding ToggleExpandedCommand}" Background="Transparent" TextColor="{AppThemeBinding Light={StaticResource TextDangerAltLight}, Dark={StaticResource TextDangerAltDark}}" Padding="0" SemanticProperties.Description="{Binding Name, StringFormat='Expand or collapse {0} additional expense.'}" />
                                                <Button Grid.Column="2" AutomationId="RemoveRetirementExpenseButton" Text="&#xf2ed;" FontFamily="FontAwesomeSolid" IsVisible="{Binding IsEditable}" Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.RemoveRetirementExpenseCommand}" CommandParameter="{Binding .}" Background="{StaticResource FixedDangerSurface}" TextColor="{StaticResource FixedDanger}" Padding="0" SemanticProperties.Description="Remove this additional retirement expense." />
                                            </Grid>
                                            <VerticalStackLayout IsVisible="{Binding IsExpanded}" IsEnabled="{Binding IsEditable}" Spacing="{StaticResource SpaceSm}">
                                            <Entry Text="{Binding Name, Mode=TwoWay}" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="Expense name" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Additional retirement expense name" />
                                            <VerticalStackLayout Spacing="3"><Label Text="Annual amount" FontAttributes="Bold" FontSize="12" /><Entry Text="{Binding AnnualAmountText, Mode=TwoWay}" Keyboard="Numeric" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="0" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Additional annual expense amount in dollars" /></VerticalStackLayout>
                                            <Grid ColumnDefinitions="*,*" ColumnSpacing="{StaticResource SpaceSm}">
                                                <VerticalStackLayout Spacing="3"><Label Text="Start age" FontAttributes="Bold" FontSize="12" /><Entry Text="{Binding StartAgeText, Mode=TwoWay}" Keyboard="Numeric" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="55" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Additional expense start age" /></VerticalStackLayout>
                                                <VerticalStackLayout Grid.Column="1" Spacing="3"><Label Text="End age" FontAttributes="Bold" FontSize="12" /><Entry AutomationId="RetirementExpenseEndAgeEntry" Text="{Binding EndAgeText, Mode=TwoWay}" Keyboard="Numeric" Background="{AppThemeBinding Light={StaticResource SurfaceCardFlatLight}, Dark={StaticResource SurfaceCardFlatDark}}" TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}" Placeholder="90" PlaceholderColor="{AppThemeBinding Light={StaticResource PlaceholderLight}, Dark={StaticResource PlaceholderDark}}" SemanticProperties.Description="Additional expense end age" /></VerticalStackLayout>
                                            </Grid>
                                            </VerticalStackLayout>
                                        </VerticalStackLayout>
                                    </Border>
                                </DataTemplate>
                            </BindableLayout.ItemTemplate>
                        </VerticalStackLayout>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Border>

                <Border Padding="{StaticResource CardPadding}" Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}" Stroke="{AppThemeBinding Light={StaticResource BorderSubtleLight}, Dark={StaticResource BorderSubtleDark}}" StrokeShape="RoundRectangle 8">
                    <VerticalStackLayout Spacing="{StaticResource SpaceSm}">
                        <Label Text="Cash-flow outlook" Style="{StaticResource CalculatorSectionHeading}" /><Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto,Auto" ColumnSpacing="{StaticResource SpaceSm}" RowSpacing="{StaticResource SpaceSm}"><VerticalStackLayout Spacing="2"><Label Text="Current balance" FontSize="12" /><Label Text="{Binding RetirementCurrentBalanceText}" FontAttributes="Bold" FontSize="18" /><Label Text="Today's dollars" FontSize="11" /></VerticalStackLayout><VerticalStackLayout Grid.Column="1" Spacing="2"><Label Text="At semi-retirement" FontSize="12" /><Label Text="{Binding RetirementBalanceAtSemiText}" FontAttributes="Bold" FontSize="18" /><Label Text="{Binding RetirementBalanceBasisText}" FontSize="11" /></VerticalStackLayout><VerticalStackLayout Grid.Row="1" Spacing="2"><Label Text="Ending balance" FontSize="12" /><Label Text="{Binding RetirementEndingBalanceText}" FontAttributes="Bold" FontSize="18" /><Label Text="{Binding RetirementEndingBasisText}" FontSize="11" /></VerticalStackLayout><VerticalStackLayout Grid.Row="1" Grid.Column="1" Spacing="2"><Label Text="First shortfall" FontSize="12" /><Label AutomationId="RetirementFirstShortfallLabel" Text="{Binding RetirementFirstShortfallText}" FontAttributes="Bold" FontSize="18" /><Label Text="First retirement year the reachable accounts cannot cover expenses" FontSize="11" /></VerticalStackLayout><VerticalStackLayout Grid.Row="2" Grid.ColumnSpan="2" Spacing="2"><Label Text="Funded years" FontSize="12" /><Label Text="{Binding RetirementFundedYearsText}" FontAttributes="Bold" FontSize="16" /></VerticalStackLayout></Grid><Border IsVisible="{Binding HasRetirementPolicyExcess}" Padding="{StaticResource CardPaddingCompact}" Background="{AppThemeBinding Light={StaticResource SurfaceWarningMutedLight}, Dark={StaticResource SurfaceWarningMutedDark}}" Stroke="{AppThemeBinding Light={StaticResource BorderWarningMutedLight}, Dark={StaticResource BorderWarningMutedDark}}" StrokeShape="RoundRectangle 8"><Label Text="{Binding RetirementPolicyExcessText}" FontSize="12" /></Border><Label Text="Income and withdrawal figures are spendable after-tax dollars using a flat estimated rate per account, not a bracket calculation. Balances shown for future ages are future dollars." FontSize="11" /><controls:InteractiveCartesianChartView Series="{Binding ProjectionSeries}" XAxes="{Binding ProjectionXAxes}" AnimationsSpeed="{Binding ChartAnimationsSpeed}" ChartHeight="300" SemanticProperties.Description="{Binding ProjectionChartDescription}" /><Button AutomationId="AnnualCashFlowDetailsButton" Text="View annual cash-flow details" Command="{Binding ViewRetirementAnnualDetailsCommand}" Background="{StaticResource FixedWarningSurface}" TextColor="{StaticResource FixedWarning}" SemanticProperties.Description="Open expandable annual retirement cash-flow details."><Button.ImageSource><FontImageSource FontFamily="FontAwesomeSolid" Glyph="&#xf03a;" Color="{StaticResource FixedWarning}" Size="14" /></Button.ImageSource></Button></VerticalStackLayout></Border>
                <Border Padding="{StaticResource CardPadding}" Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}" Stroke="{AppThemeBinding Light={StaticResource BorderSubtleLight}, Dark={StaticResource BorderSubtleDark}}" StrokeShape="RoundRectangle 8">
                    <VerticalStackLayout Spacing="{StaticResource SpaceSm}">
                        <Label Text="Account balances over time" Style="{StaticResource CalculatorSectionHeading}" />
                        <VerticalStackLayout Spacing="{StaticResource SpaceSm}">
                            <controls:InteractiveCartesianChartView Series="{Binding RetirementBucketSeries}" XAxes="{Binding ProjectionXAxes}" AnimationsSpeed="{Binding ChartAnimationsSpeed}" ChartHeight="300" SemanticProperties.Description="{Binding RetirementBucketDescription}" />
                            <Label Text="{Binding RetirementBucketSummary}" TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}" FontSize="13" LineHeight="1.3" />
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Border>
                <Border Padding="{StaticResource CardPadding}" Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}" Stroke="{AppThemeBinding Light={StaticResource BorderSubtleLight}, Dark={StaticResource BorderSubtleDark}}" StrokeShape="RoundRectangle 8">
                    <VerticalStackLayout Spacing="{StaticResource SpaceSm}">
                        <Label Text="Save or export this plan" Style="{StaticResource CalculatorSectionHeading}" />
                        <Entry Text="{Binding PlanNameText}" Background="{AppThemeBinding Light={StaticResource SurfacePageLight}, Dark={StaticResource SurfacePageDark}}" Placeholder="My Retirement Cash Flow Plan" SemanticProperties.Description="Name for this saved Retirement Cash Flow plan" />
                        <Button Text="Save and return"
                                Command="{Binding SavePlanAndCloseCommand}"
                                SemanticProperties.Description="Save this retirement cash-flow plan and return to the previous screen." />
                        <Button Text="Export to Excel" Command="{Binding ExportCommand}" Background="{StaticResource Secondary}" TextColor="{StaticResource PrimaryDarkText}" SemanticProperties.Description="Create a Retirement Cash Flow workbook on this device and open the native share sheet." />
                        <Label IsVisible="{Binding HasPlanStatusMessage}" Text="{Binding PlanStatusMessage}" Style="{StaticResource CalculatorSupportingText}" />
                        <Label IsVisible="{Binding HasExportStatusMessage}" Text="{Binding ExportStatusMessage}" Style="{StaticResource CalculatorSupportingText}" />
                        <controls:EducationalDisclaimerView />
                    </VerticalStackLayout>
                </Border>
                <controls:CalculationExplanationView Explanation="For each age, inflation multiplier = (1 + inflation rate)^(age - current age).&#10;&#10;Each account first grows by its annual return; before semi-retirement, its contribution is added after being increased by the inflation multiplier.&#10;&#10;Active income = annual amount x (1 + income growth)^(age - current age), reduced by the tax rate when it is not already after-tax.&#10;&#10;Expenses = annual expenses x inflation multiplier plus active additional expenses. The remaining spending gap is expenses - outside income - deferred income.&#10;&#10;Withdrawals are grossed up for each account's tax rate, limited by its balance and withdrawal-rate policy, and account balances are reduced by gross withdrawals." />
        </VerticalStackLayout>
    </ScrollView>
</views:CalculatorPageBase>

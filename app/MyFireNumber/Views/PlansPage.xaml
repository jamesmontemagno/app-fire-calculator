<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:MyFireNumber.ViewModels"
             x:Class="MyFireNumber.Views.PlansPage"
             x:DataType="viewModels:PlansViewModel"
             Title="Plans"
             Background="{AppThemeBinding Light=#F5F7F2, Dark=#15211C}"
             SafeAreaEdges="Container">

    <Grid Padding="{StaticResource PagePaddingCompact}" RowDefinitions="Auto,*" RowSpacing="{StaticResource SpaceMd}">
        <VerticalStackLayout Spacing="{StaticResource SpaceMd}">
            <Grid ColumnDefinitions="32,*" ColumnSpacing="{StaticResource SpaceSm}">
                        <Label Text="&#xf15c;"
                               FontFamily="FontAwesomeSolid"
                               WidthRequest="32"
                               HeightRequest="32"
                               HorizontalTextAlignment="Center"
                               VerticalTextAlignment="Center"
                               TextColor="{AppThemeBinding Light=#326B57, Dark=#9ACCB3}"
                               FontSize="24"
                               SemanticProperties.Description="Saved plans" />
                        <VerticalStackLayout Grid.Column="1" Spacing="2">
                            <Label Text="Saved plans"
                                   TextColor="{AppThemeBinding Light=#17352C, Dark=#F2F7F3}"
                                   FontAttributes="Bold"
                                   FontSize="24"
                                   SemanticProperties.HeadingLevel="Level1" />
                            <Label Text="Your scenarios stay on this device."
                                   TextColor="{AppThemeBinding Light=#425D54, Dark=#B9D1C2}"
                                   FontSize="13" />
                        </VerticalStackLayout>
                    </Grid>
            <SearchBar AutomationId="PlanSearch"
                       Placeholder="Search saved plans"
                       Text="{Binding SearchText, Mode=TwoWay}"
                       Background="{AppThemeBinding Light=#F5F7F2, Dark=#15211C}"
                       TextColor="{AppThemeBinding Light=#17352C, Dark=#F2F7F3}"
                       PlaceholderColor="{AppThemeBinding Light=#587068, Dark=#B9D1C2}"
                       SemanticProperties.Description="Search saved plans by name." />
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="{StaticResource SpaceSm}">
                <Label Text="Sort"
                       VerticalTextAlignment="Center"
                       TextColor="{AppThemeBinding Light=#425D54, Dark=#B9D1C2}"
                       FontAttributes="Bold"
                       FontSize="13" />
                <Picker AutomationId="PlanSort"
                        Grid.Column="1"
                        ItemsSource="{Binding SortOptions}"
                        ItemDisplayBinding="{Binding Name}"
                        SelectedItem="{Binding SelectedSortOption, Mode=TwoWay}"
                        Background="Transparent"
                        TextColor="{AppThemeBinding Light=#17352C, Dark=#F2F7F3}"
                        TitleColor="{AppThemeBinding Light=#587068, Dark=#B9D1C2}"
                        SemanticProperties.Description="Sort saved plans." />
            </Grid>
        </VerticalStackLayout>
        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Plans}"
                        SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="viewModels:PlanListItem">
                   <SwipeView Margin="0,0,0,10">
                       <SwipeView.RightItems>
                           <SwipeItems Mode="Reveal">
                               <SwipeItemView Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:PlansViewModel}}, Path=RenamePlanCommand}"
                                              CommandParameter="{Binding .}"
                                              SemanticProperties.Description="Rename this saved plan.">
                                   <Border WidthRequest="84"
                                           Padding="8,6"
                                           Background="#326B57"
                                           StrokeThickness="0"
                                           StrokeShape="RoundRectangle 12">
                                       <VerticalStackLayout Spacing="3"
                                                            HorizontalOptions="Center"
                                                            VerticalOptions="Center">
                                           <Label Text="&#xf044;"
                                                  FontFamily="FontAwesomeSolid"
                                                  FontSize="17"
                                                  HorizontalTextAlignment="Center"
                                                  TextColor="#FFFFFF" />
                                           <Label Text="Rename"
                                                  FontAttributes="Bold"
                                                  FontSize="12"
                                                  HorizontalTextAlignment="Center"
                                                  TextColor="#FFFFFF" />
                                       </VerticalStackLayout>
                                   </Border>
                               </SwipeItemView>
                               <SwipeItemView Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:PlansViewModel}}, Path=DuplicatePlanCommand}"
                                              CommandParameter="{Binding .}"
                                              SemanticProperties.Description="Duplicate this saved plan.">
                                   <Border WidthRequest="84"
                                           Padding="8,6"
                                           Background="#426B9D"
                                           StrokeThickness="0"
                                           StrokeShape="RoundRectangle 12">
                                       <VerticalStackLayout Spacing="3"
                                                            HorizontalOptions="Center"
                                                            VerticalOptions="Center">
                                           <Label Text="&#xf0c5;"
                                                  FontFamily="FontAwesomeSolid"
                                                  FontSize="17"
                                                  HorizontalTextAlignment="Center"
                                                  TextColor="#FFFFFF" />
                                           <Label Text="Copy"
                                                  FontAttributes="Bold"
                                                  FontSize="12"
                                                  HorizontalTextAlignment="Center"
                                                  TextColor="#FFFFFF" />
                                       </VerticalStackLayout>
                                   </Border>
                               </SwipeItemView>
                               <SwipeItemView Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:PlansViewModel}}, Path=DeletePlanCommand}"
                                              CommandParameter="{Binding .}"
                                              SemanticProperties.Description="Delete this saved plan after confirmation.">
                                   <Border WidthRequest="84"
                                           Padding="8,6"
                                           Background="#A33A31"
                                           StrokeThickness="0"
                                           StrokeShape="RoundRectangle 12">
                                       <VerticalStackLayout Spacing="3"
                                                            HorizontalOptions="Center"
                                                            VerticalOptions="Center">
                                           <Label Text="&#xf2ed;"
                                                  FontFamily="FontAwesomeSolid"
                                                  FontSize="17"
                                                  HorizontalTextAlignment="Center"
                                                  TextColor="#FFFFFF" />
                                           <Label Text="Delete"
                                                  FontAttributes="Bold"
                                                  FontSize="12"
                                                  HorizontalTextAlignment="Center"
                                                  TextColor="#FFFFFF" />
                                       </VerticalStackLayout>
                                   </Border>
                               </SwipeItemView>
                           </SwipeItems>
                       </SwipeView.RightItems>
                       <Border Padding="{StaticResource CardPadding}"
                               Background="{AppThemeBinding Light=#FFFFFF, Dark=#203129}"
                               Stroke="{AppThemeBinding Light=#D7DED8, Dark=#365244}"
                               StrokeShape="RoundRectangle 12"
                               SemanticProperties.Description="{Binding Name, StringFormat='Saved plan {0}.'}">
                           <Grid ColumnDefinitions="*,Auto" ColumnSpacing="{StaticResource SpaceMd}">
                               <Grid.GestureRecognizers>
                                   <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:PlansViewModel}}, Path=OpenPlanCommand}"
                                                         CommandParameter="{Binding .}" />
                               </Grid.GestureRecognizers>
                               <VerticalStackLayout Spacing="3">
                                   <Label Text="{Binding Name}"
                                          TextColor="{AppThemeBinding Light=#17352C, Dark=#F2F7F3}"
                                          FontAttributes="Bold"
                                          FontSize="18" />
                                   <Label Text="{Binding CalculatorTitle}"
                                          TextColor="{AppThemeBinding Light=#326B57, Dark=#9ACCB3}"
                                          FontSize="13" />
                                   <Label Text="{Binding UpdatedDescription}"
                                          TextColor="{AppThemeBinding Light=#587068, Dark=#B9D1C2}"
                                          FontSize="12" />
                               </VerticalStackLayout>
                               <Label Grid.Column="1"
                                      Text="&#xf054;"
                                      FontFamily="FontAwesomeSolid"
                                      FontSize="14"
                                      VerticalTextAlignment="Center"
                                      TextColor="{AppThemeBinding Light=#587068, Dark=#B9D1C2}"
                                      SemanticProperties.Description="Open" />
                           </Grid>
                       </Border>
                   </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            <CollectionView.EmptyView>
                <VerticalStackLayout VerticalOptions="Center" Spacing="{StaticResource SpaceSm}">
                    <Label Text="&#xf15c;"
                           FontFamily="FontAwesomeSolid"
                           HorizontalTextAlignment="Center"
                           TextColor="{AppThemeBinding Light=#326B57, Dark=#9ACCB3}"
                           FontSize="34"
                           SemanticProperties.Description="No saved plans" />
                    <Label Text="No saved plans yet."
                           HorizontalTextAlignment="Center"
                           FontAttributes="Bold"
                           FontSize="18" />
                    <Label Text="Save a calculator scenario to compare it here."
                           HorizontalTextAlignment="Center"
                           TextColor="{AppThemeBinding Light=#425D54, Dark=#B9D1C2}"
                           LineHeight="1.3" />
                </VerticalStackLayout>
            </CollectionView.EmptyView>
        </CollectionView>
        <ActivityIndicator IsRunning="{Binding IsLoading}"
                           IsVisible="{Binding IsLoading}"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"
                           SemanticProperties.Description="Loading saved plans." />
    </Grid>
</ContentPage>
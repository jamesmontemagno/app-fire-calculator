<?xml version="1.0" encoding="utf-8" ?>
<views:CalculatorPageBase xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                          xmlns:controls="clr-namespace:MyFireNumber.Views.Controls"
                          xmlns:views="clr-namespace:MyFireNumber.Views"
                          xmlns:viewModels="clr-namespace:MyFireNumber.ViewModels"
                          xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
                          x:Class="MyFireNumber.Views.WithdrawalRatePage"
                          x:DataType="viewModels:WithdrawalRateViewModel"
                          Title="{Binding Title}"
                          Shell.NavBarIsVisible="True"
                          Background="{AppThemeBinding Light={StaticResource SurfacePageLight}, Dark={StaticResource SurfacePageDark}}"
                          SafeAreaEdges="All">
    <ContentPage.ToolbarItems>
        <ToolbarItem Command="{Binding SavePlanCommand}"
                     SemanticProperties.Description="Save the current calculator values as a plan.">
            <ToolbarItem.IconImageSource>
                <FontImageSource FontFamily="FontAwesomeSolid" Glyph="&#xf0c7;" Size="18" />
            </ToolbarItem.IconImageSource>
        </ToolbarItem>
    </ContentPage.ToolbarItems>
    <ScrollView>
        <VerticalStackLayout Padding="{StaticResource PagePadding}" Spacing="{StaticResource SpaceLg}">
            <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" HorizontalOptions="Center" SemanticProperties.Description="Restoring your local calculator draft." />
            <Border IsVisible="{Binding HasValidationMessage}" Padding="14" Background="{AppThemeBinding Light={StaticResource SurfaceWarningLight}, Dark={StaticResource SurfaceWarningDark}}" Stroke="{AppThemeBinding Light={StaticResource BorderWarningLight}, Dark={StaticResource BorderWarningDark}}" StrokeShape="RoundRectangle 8"><Label Text="{Binding ValidationMessage}" TextColor="{AppThemeBinding Light={StaticResource TextWarningLight}, Dark={StaticResource TextWarningDark}}" LineHeight="1.3" /></Border>

            <controls:CalculatorInfoView Title="What is a withdrawal rate?"
                                         Summary="Test how long a portfolio may last while funding inflation-adjusted retirement spending."
                                         Details="A withdrawal rate is the first-year percentage spent from your portfolio. The 4% rule is a common starting point, not a guarantee."
                                         IconGlyph="&#xf02d;" />

            <Border Padding="{StaticResource CardPadding}" Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}" Stroke="{AppThemeBinding Light={StaticResource BorderSubtleLight}, Dark={StaticResource BorderSubtleDark}}" StrokeShape="RoundRectangle 8">
                <VerticalStackLayout Spacing="{StaticResource SpaceMd}">
                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="{StaticResource SpaceSm}"><Label Text="Your portfolio" Style="{StaticResource CalculatorSectionHeading}" /><Button Grid.Column="1" Text="Reset" Command="{Binding ResetDefaultsCommand}" Background="{StaticResource Secondary}" TextColor="{StaticResource PrimaryDarkText}" FontSize="12" SemanticProperties.Description="Reset Withdrawal Rate values to their defaults." /></Grid>
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="{StaticResource SpaceMd}">
                        <controls:NumericInputView Header="Portfolio value" HelpText="Current total invested portfolio, in dollars." Placeholder="1000000" Text="{Binding PortfolioValueText}" />
                        <controls:NumericInputView Grid.Column="1" Header="Duration" HelpText="Number of retirement years to model." Placeholder="30" Text="{Binding RetirementYearsText}" />
                    </Grid>
                    <controls:AdvancedAssumptionsExpander>
                        <Grid Margin="0,12,0,0" ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="{StaticResource SpaceMd}" RowSpacing="{StaticResource SpaceMd}">
                            <controls:PercentageInputView Header="Withdrawal rate" HelpText="First-year percentage withdrawn from the portfolio. This is an assumption, not a promise." Placeholder="4" Minimum="2" Maximum="8" Text="{Binding WithdrawalRateText}" />
                            <controls:PercentageInputView Grid.Column="1" Header="Expected return" HelpText="Expected average annual investment return before inflation." Placeholder="7" Minimum="0" Maximum="15" Text="{Binding ExpectedReturnText}" />
                            <controls:PercentageInputView Grid.Row="1" Grid.ColumnSpan="2" Header="Inflation" HelpText="Expected annual increase in the cost of living." Placeholder="3" Minimum="0" Maximum="10" Text="{Binding InflationRateText}" />
                        </Grid>
                    </controls:AdvancedAssumptionsExpander>
                </VerticalStackLayout>
            </Border>

            <VerticalStackLayout Spacing="{StaticResource SpaceSm}">
                <Label Text="Your withdrawal outlook" Style="{StaticResource CalculatorSectionHeading}" />
                <Label Text="{Binding WithdrawalStatusText}" Style="{StaticResource CalculatorStatusHeadline}" SemanticProperties.Description="{Binding WithdrawalStatusText}" />
                <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="{StaticResource SpaceSm}" RowSpacing="{StaticResource SpaceSm}">
                    <Border Padding="{StaticResource CardPaddingCompact}" Background="{AppThemeBinding Light={StaticResource SurfaceInfoSkyLight}, Dark={StaticResource SurfaceInfoSkyDark}}" StrokeThickness="0" StrokeShape="RoundRectangle 8"><VerticalStackLayout Spacing="2"><Label Text="Annual withdrawal" FontSize="12" /><Label Text="{Binding WithdrawalAnnualText}" FontAttributes="Bold" FontSize="22" /></VerticalStackLayout></Border>
                    <Border Grid.Column="1" Padding="{StaticResource CardPaddingCompact}" Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}" StrokeShape="RoundRectangle 8"><VerticalStackLayout Spacing="2"><Label Text="Monthly withdrawal" FontSize="12" /><Label Text="{Binding WithdrawalMonthlyText}" FontAttributes="Bold" FontSize="18" /></VerticalStackLayout></Border>
                    <Border Grid.Row="1" Padding="{StaticResource CardPaddingCompact}" Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}" StrokeShape="RoundRectangle 8"><VerticalStackLayout Spacing="2"><Label Text="Portfolio lasts" FontSize="12" /><Label Text="{Binding WithdrawalLongevityText}" FontAttributes="Bold" FontSize="18" /></VerticalStackLayout></Border>
                    <Border Grid.Row="1" Grid.Column="1" Padding="{StaticResource CardPaddingCompact}" Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}" StrokeShape="RoundRectangle 8"><VerticalStackLayout Spacing="2"><Label Text="Share of horizon funded" FontSize="12" /><Label Text="{Binding WithdrawalHorizonFundedText}" FontAttributes="Bold" FontSize="18" /></VerticalStackLayout></Border>
                </Grid>
                <Label Text="{Binding WithdrawalRateAnalysisText}" Style="{StaticResource CalculatorSupportingText}" />
            </VerticalStackLayout>

            <VerticalStackLayout Spacing="{StaticResource SpaceSm}">
                <Label Text="Portfolio balance over time" Style="{StaticResource CalculatorSectionHeading}" />
                <lvc:CartesianChart Series="{Binding ProjectionSeries}" XAxes="{Binding ProjectionXAxes}" AnimationsSpeed="{Binding ChartAnimationsSpeed}" HeightRequest="300" SemanticProperties.Description="{Binding ProjectionChartDescription}" />
            </VerticalStackLayout>

            <Border Padding="{StaticResource CardPadding}" Background="{AppThemeBinding Light={StaticResource SurfaceCardLight}, Dark={StaticResource SurfaceCardDark}}" Stroke="{AppThemeBinding Light={StaticResource BorderSubtleLight}, Dark={StaticResource BorderSubtleDark}}" StrokeShape="RoundRectangle 8">
                <VerticalStackLayout Spacing="{StaticResource SpaceSm}">
                    <Label Text="Save or export this plan" Style="{StaticResource CalculatorSectionHeading}" />
                    <Entry Text="{Binding PlanNameText}" Placeholder="My Withdrawal Plan" Background="{AppThemeBinding Light={StaticResource SurfacePageLight}, Dark={StaticResource SurfacePageDark}}" SemanticProperties.Description="Name for this saved Withdrawal Rate plan" />
                    <Button Text="{Binding SavePlanActionText}" Command="{Binding SavePlanCommand}" SemanticProperties.Description="{Binding SavePlanActionDescription}" />
                    <Button Text="Export to Excel" Command="{Binding ExportCommand}" Background="{StaticResource Secondary}" TextColor="{StaticResource PrimaryDarkText}" SemanticProperties.Description="Create a Withdrawal Rate workbook on this device and open the native share sheet." />
                    <Label IsVisible="{Binding HasPlanStatusMessage}" Text="{Binding PlanStatusMessage}" Style="{StaticResource CalculatorSupportingText}" />
                    <Label IsVisible="{Binding HasExportStatusMessage}" Text="{Binding ExportStatusMessage}" Style="{StaticResource CalculatorSupportingText}" />
                    <controls:EducationalDisclaimerView />
                </VerticalStackLayout>
            </Border>
            <controls:CalculationExplanationView Explanation="Annual withdrawal = starting portfolio x withdrawal rate.&#10;&#10;Monthly withdrawal = annual withdrawal / 12.&#10;&#10;Each year, balance = prior balance x (1 + expected return) - inflation-adjusted withdrawal, and next year's withdrawal = prior withdrawal x (1 + inflation rate).&#10;&#10;Portfolio longevity is the number of complete funded years before the balance reaches zero. This is a deterministic projection, not a historical or Monte Carlo probability." />
        </VerticalStackLayout>
    </ScrollView>
</views:CalculatorPageBase>
